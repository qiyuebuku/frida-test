# Context Bundle 模板

> Research Phase 读取知识库后，按此模板生成 `{KB_DIR}/drafts/context_bundle_ch{NNNN}.md`。
> 总字数控制在 ~3000 字以内。每个 Section 标注了字数预算。

---

# Context Bundle — ch{NNNN}

## S1. 故事位置（~150 字）

- **所属弧**：{弧名}（arc_{XX}）— {弧的一句话概括}
- **主线进展**：
  - {主线1名称}：{当前进展到哪里，一句话}
  - {主线2名称}：{当前进展到哪里，一句话}
- **上一章尾**（原文最后 2-3 句直接复制）：
  > {从 text/ch{N-1}.md 复制最后 2-3 句原文}
- **本章方向**：{综合 arc + plot_lines 提炼的本章核心推进目标}
- **用户提示**：{--hint 内容，无则写"无"}

## S2. 场景角色（~300 字）

### 出场角色简表

| 角色 | 当前状态 | 说话特征 |
|------|----------|----------|
| {角色1} | {当前处境/情绪/能力状态} | {说话方式简述，如"沉稳、少言"} |
| {角色2} | {当前处境/情绪/能力状态} | {说话方式简述} |
| ... | ... | ... |

### 伏笔状态

- **待回收**（本章适合回收的 1-2 条）：
  - {伏笔描述}（来源：ch{XXXX}，open_loops 编号）
- **新铺建议**（0-1 条）：
  - {建议铺设的新伏笔}

## S3. 风格硬指标（~100 字）

> 以下数值从 `style/.build/stats.json` 和 `style/rhythm.md` 中提取，**不硬编码固定值**。

| 指标 | 目标值 | 红线 | 数据来源 |
|------|--------|------|----------|
| 总字数 | {从 stats.json 推导} | {基线 ×0.8} | stats.json → total_chars / total_chapters |
| 平均句长 | {基线 ±20%} | {基线 ×0.7} | stats.json → sentence.avg_length |
| 对话占比 | {基线 ±30%} | {超出基线 ×1.5} | stats.json → dialogue.ratio |
| 对话标签偏好 | {从 narrative.md 提取} | {占比低于基线 ×0.8} | narrative.md → 对话特征 |
| 禁忌词数量 | 0 | >0 | vocabulary.md → 禁忌词列表 |
| 章首类型 | {从 rhythm.md 提取} | 不匹配偏好 | rhythm.md → 章首类型分布 |
| 章尾类型 | {从 rhythm.md 提取} | 不匹配偏好 | rhythm.md → 章尾类型分布 |

## S4. 禁忌速查（~200 字）

> 以下内容从 `style/vocabulary.md` 和 `style/narrative.md` 中提取，**每本书的禁忌不同**。

### 绝对禁止

**{类别1}**：{从 vocabulary.md 的禁忌词列表中提取}

**{类别2}**：{从 vocabulary.md 的时间/称呼/语气约束中提取}

**{类别3}**：{从 narrative.md 的情感表达约束中提取}

### 替代公式

- {从 narrative.md 中提取的替代策略}
- 每句至少承载 2 种信息（状态+背景 或 动作+情绪）
- 内心独白连续不超过 3 句，之后必须穿插动作或环境
- 旁白评价点到即止（一句话够了）

### 特殊角色规则

- {从 guide.md 中提取的特殊角色用词规则，如穿越者可用现代词汇等}

## S5. 上一章衔接（~300 字）

> {上一章最后一个场景的完整段落，从 text/ch{N-1}.md 原文直接复制}
> {包含该场景的环境、出场人物、最后的动作或对话}
> {确保续写开头能自然接上}
