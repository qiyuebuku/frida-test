# 智能基金服务 — 系统架构设计

基于简化 DDD 分层架构，清晰划分 接口 → 应用 → 领域 → 基础设施 四层。

---

## 1. 架构总览

```
smart-fund-server/
├── main.py                                    # 启动入口
├── src/
│
│   ┌──────────────────────────────────────────────────────────┐
│   │          src/interfaces/ — 接口层                         │
│   │   api/routes/     │ tasks/            │ cli/             │
│   │   (FastAPI 路由)   │ (jettask task)    │ (管理命令)       │
│   └────────────────────────┬─────────────────────────────────┘
│                            ↓
│   ┌──────────────────────────────────────────────────────────┐
│   │          src/application/ — 应用层                        │
│   │   ┌──────────────────┐  ┌──────────────────────────┐     │
│   │   │ services/         │  │ orchestrators/            │     │
│   │   │ dto/              │  │ (长驻进程，有状态)        │     │
│   │   │ (用例编排/DTO)    │  │                            │     │
│   │   │ AggregationSvc    │  │ TaskOrchestrator          │     │
│   │   │ TradeSvc          │  │ (tmux 会话生命周期)       │     │
│   │   │ QuerySvc          │  │                           │     │
│   │   │ KnowledgeSvc      │  │                           │     │
│   │   │ AdapterRegistry   │  │                           │     │
│   │   └──────────────────┘  └──────────────────────────┘     │
│   └────────────────────────┬─────────────────────────────────┘
│                            ↓
│   ┌──────────────────────────────────────────────────────────┐
│   │          src/domain/ — 领域层                             │
│   │   aggregation/ ⭐ │ knowledge/ │ trading/  │ task/      │
│   │   (数据聚合)      │ (知识层)   │ (交易)    │ (任务)     │
│   └────────────────────────┬─────────────────────────────────┘
│                            ↓
│   ┌──────────────────────────────────────────────────────────┐
│   │          src/infrastructure/ — 基础设施层                  │
│   │   clients/(10) │ db/(4) │ tools/ │ config/ │ tmux/      │
│   └──────────────────────────────────────────────────────────┘
│
│   src/common/ — 通用模块 (types, exceptions)
│
├── tests/
└── docs/
```

**依赖规则**：
- ✅ interfaces → application → domain
- ✅ application → infrastructure（应用层编排外部能力）
- ✅ 所有层 → common
- ❌ domain 不直接依赖 infrastructure
- ❌ domain 不依赖 interfaces
- ❌ interfaces 不跳过 application 直接调 domain
- ❌ interfaces 不直接操作 DB、repository 或外部客户端
- ❌ interfaces 不直接实例化 Compiler、Domain Adapter 或 Repository
- ❌ domain 不引入 FastAPI、click、SQLAlchemy session、环境变量读取

---

## 2. 当前问题

### 2.1 目录结构混乱

```
当前结构（扁平，职责不清）：

smart-fund-server/
├── main.py                    ← 启动 + 生命周期，职责还行
├── config.py                  ← ✅ 刚刚统一的配置
├── routers/                   ← 接口层（但直接操作 DB、实例化 service）
│   ├── task.py                ← 直接 import task_db、executor
│   ├── trade.py               ← 直接构造 HTTP 请求
│   └── ...
├── services/                  ← 什么都往里塞
│   ├── task_executor.py       ← 600+ 行巨型文件（tmux + Claude + 状态管理）
│   ├── clients/               ← 基础设施层（10 个外部 API 客户端）
│   ├── aggregators/           ← 领域层（数据聚合业务逻辑）
│   ├── handlers/              ← 应用层（OCR、chat 处理）
│   ├── trade/                 ← 领域层（交易风控、指标）
│   ├── db/                    ← 基础设施层（数据库 CRUD）
│   └── tools/                 ← 基础设施层（OCR、调度器、浏览器）
├── tasks/                     ← 接口层（jettask task 入口）
└── tests/                     ← 测试
```

**核心问题**：
1. **services/ 混合了三个层**：clients(基础设施) + aggregators(领域) + handlers(应用) 平铺在一起
2. **routers 太厚**：直接操作 DB、构造请求，缺少应用层中转
3. **task_executor.py 是巨型单体**：tmux 管理 + Claude 交互 + 任务状态 + 模型切换全塞一个文件
4. **没有应用层**：router 直接调 service/db，跳过了用例编排

### 2.2 依赖混乱

```
当前依赖关系（网状）：

routers ──→ services/db (直接查 DB)
routers ──→ services/task_executor (直接操作执行器)
routers ──→ services/clients (直接调客户端)
services/aggregators ──→ services/clients (OK，但缺乏抽象)
services/trade ──→ services/db (直接查 DB)
services/handlers ──→ services/tools (OK)
```

应该是：
```
routers → AppService → Domain → Infrastructure
```

---

## 3. 领域设计

### 3.1 五个领域

```
              ┌──────────────────────┐
              │  Aggregation（聚合）  │  ← 系统心脏，数据驱动一切
              │ 新闻/资金流/宏观/情绪 │
              │ 市场数据/事件反馈     │
              └──────────┬───────────┘
                         │ 提供材料
              ┌──────────▼───────────┐
              │ Knowledge（知识层）   │  ← 通用知识基础设施
              │ 节点/边/证据/检索     │
              └──────────┬───────────┘
                         │ 提供语义上下文
          ┌──────────────┼──────────────┐
          │              │              │
┌─────────┴─────┐ ┌─────┴──────┐ ┌────┴──────────┐
│ Trading（交易）│ │ Query（查询）│ │ Task（任务执行）│
│ 决策/风控/复盘 │ │ 基金/市场   │ │ Claude/OCR    │
└───────────────┘ └────────────┘ └───────────────┘
```

| 领域 | 定位 | 核心模块 | 代码位置 | 业务文档 |
|------|------|---------|---------|---------|
| **Aggregation** ⭐ | 数据采集与聚合 | 6 个聚合 service + normalize + helpers | `src/domain/aggregation/` | [数据聚合](../3.%20实施方案/2.%20数据聚合/) |
| **Knowledge** | 通用知识基础设施 | Compiler、Adapter 协议、Fact Store 协议、Wiki、Retrieval、Quality | `src/domain/knowledge/` | [知识图谱](../3.%20实施方案/4.%20知识图谱/) |
| **Trading** | 交易决策与执行 | 风控、指标、决策复盘 | `src/domain/trading/` | — |
| **Task** | 异步任务执行 | Claude 交互、OCR、长任务管理 | `src/domain/task/` | — |
| **Query** | 数据查询 | 基金查询、市场数据、策略 | `src/application/services/query_service.py` | — |

### 3.2 基础设施

| 模块 | 职责 | 代码位置 | 业务文档 |
|------|------|---------|---------|
| **clients/** | 10 个外部 API 客户端 | `src/infrastructure/clients/` | [数据源对接](../3.%20实施方案/1.%20数据源对接/) |
| **db/** | PostgreSQL 数据库操作 | `src/infrastructure/db/` | — |
| **persistence/** | SQLAlchemy model 与 repository | `src/infrastructure/persistence/` | — |
| **tools/** | OCR、浏览器、调度器 | `src/infrastructure/tools/` | — |
| **config/** | 统一配置（环境变量覆盖） | `src/infrastructure/config/` | — |
| **tmux/** | tmux 会话适配器 | `src/infrastructure/tmux/` | — |

---

## 4. 目标目录结构

参考 jettask_v2 的 `src/` 分层结构：所有业务代码放 `src/` 下，按 `interfaces → application → domain → infrastructure → common` 分层。`main.py`、`tests/`、`docs/` 与 `src/` 平级。

```
smart-fund-server/
├── main.py                                    # 启动入口
│
├── src/                                       # ── 所有业务代码 ──
│   ├── __init__.py
│   │
│   ├── interfaces/                            # ── 接口层 ──
│   │   ├── __init__.py
│   │   ├── api/                               # FastAPI 路由（薄层）
│   │   │   ├── __init__.py                    # 路由注册
│   │   │   ├── routes/                        # 按领域分组
│   │   │   │   ├── aggregation.py             # /api/news, /api/market-flow
│   │   │   │   ├── knowledge.py               # /api/kg/...
│   │   │   │   ├── trade.py                   # /api/trade/...
│   │   │   │   ├── task.py                    # /api/task/...
│   │   │   │   ├── query.py                   # /api/fund, /api/market, /api/strategy
│   │   │   │   ├── file.py                    # /api/file/...
│   │   │   │   ├── terminal.py                # /ws/terminal (WebSocket)
│   │   │   │   └── spy.py                     # /api/spy
│   │   │   └── middleware/                     # 中间件（CORS、错误处理等）
│   │   ├── tasks/                             # jettask task 入口（薄层）
│   │   │   ├── __init__.py                    # Jettask app 实例
│   │   │   └── collection_tasks.py            # 6 个聚合 task
│   │   └── cli/                               # 命令行管理工具
│   │       ├── __init__.py
│   │       ├── knowledge.py                   # kg compile/query/rebuild/quality
│   │       └── register_schedules.py          # Schedule 注册（一次性管理操作）
│   │
│   ├── application/                           # ── 应用层 ──
│   │   ├── __init__.py
│   │   ├── dto/                               # 用例输入/输出 DTO
│   │   │   ├── knowledge_dto.py               # KG API/CLI 共用 DTO
│   │   │   ├── extraction_dto.py
│   │   │   ├── collection_dto.py
│   │   │   └── trading_dto.py
│   │   ├── services/                          # 用例编排（无状态）
│   │   │   ├── aggregation_service.py         # 聚合：触发采集、查询、重放
│   │   │   ├── knowledge_service.py           # 知识：编译、检索、Wiki、质量扫描
│   │   │   ├── knowledge_source_projection_service.py # 知识：业务表投影为 Source Record
│   │   │   ├── knowledge_adapter_registry.py  # adapter 注册与选择
│   │   │   ├── trade_service.py               # 交易：决策、下单、复盘
│   │   │   ├── query_service.py               # 查询：基金快照、市场总览
│   │   │   └── task_service.py                # 任务：创建、查询、追问
│   │   └── orchestrators/                     # 进程编排（有状态）
│   │       └── task_orchestrator.py           # tmux 会话生命周期管理
│   │
│   ├── domain/                                # ── 领域层 ──
│   │   ├── __init__.py
│   │   ├── aggregation/                       # 数据聚合领域
│   │   │   ├── __init__.py
│   │   │   ├── news.py                        # 新闻聚合逻辑
│   │   │   ├── fund_flow.py                   # 资金流聚合逻辑
│   │   │   ├── macro.py                       # 宏观政策聚合逻辑
│   │   │   ├── sentiment.py                   # 情绪舆情聚合逻辑
│   │   │   ├── market.py                      # 基础市场数据聚合逻辑
│   │   │   ├── event_feedback.py              # 事件反馈聚合逻辑
│   │   │   ├── helpers.py                     # 通用：源级间隔控制、去重
│   │   │   └── normalize.py                   # 格式转换函数
│   │   ├── knowledge/                         # 通用知识基础设施领域
│   │   │   ├── __init__.py
│   │   │   ├── schemas.py                     # 节点、边、证据、版本、输入协议
│   │   │   ├── adapter.py                     # 自定义领域 Adapter 协议
│   │   │   ├── compiler.py                    # 通用 Knowledge Compiler
│   │   │   ├── resolver.py                    # 实体归一
│   │   │   ├── evidence.py                    # 证据绑定
│   │   │   ├── retrieval.py                   # 检索协议与 RRF
│   │   │   ├── wiki.py                        # Wiki 页面协议
│   │   │   ├── quality.py                     # 图质量规则
│   │   │   └── repositories/                  # 知识层 repository 协议
│   │   │       ├── knowledge_repository.py    # kg_* 事实库协议
│   │   │       └── knowledge_source_projection_repository.py # Raw Row 读取协议
│   │   ├── knowledge_adapters/                # 领域适配器
│   │   │   └── financial/                     # 金融 KG Adapter
│   │   │       ├── adapter.py                 # 金融输入映射和抽取规则
│   │   │       ├── sources.py                 # 金融 source contract
│   │   │       ├── source_classification.py   # 金融来源分类规则
│   │   │       ├── source_projection.py       # Raw Row 到 Source Record 的纯转换
│   │   │       ├── confidence.py              # 金融置信度策略
│   │   │       └── consumption.py             # 金融消费边界
│   │   ├── trading/                           # 交易领域
│   │   │   ├── risk_manager.py                # 风控规则
│   │   │   ├── indicators.py                  # 量化指标
│   │   │   └── review.py                      # 决策复盘
│   │   └── task/                              # 任务领域
│   │       ├── handlers/                      # 任务处理器（OCR、chat、holdings）
│   │       └── models.py                      # 任务状态模型
│   │
│   ├── infrastructure/                        # ── 基础设施层 ──
│   │   ├── __init__.py
│   │   ├── config/                            # 配置管理
│   │   │   └── settings.py                    # 全局配置（环境变量覆盖）
│   │   ├── clients/                           # 外部 API 客户端（10 个）
│   │   │   ├── base.py
│   │   │   ├── cls.py
│   │   │   ├── eastmoney.py
│   │   │   ├── gov.py
│   │   │   ├── pboc.py
│   │   │   ├── sina.py
│   │   │   ├── tencent.py
│   │   │   ├── ths.py
│   │   │   ├── tiantianjijin.py
│   │   │   └── xueqiu.py
│   │   ├── db/                                # 数据库操作
│   │   │   ├── fund_db.py
│   │   │   ├── task_db.py
│   │   │   ├── ocr_db.py
│   │   │   └── raw_data.py
│   │   ├── persistence/                       # SQLAlchemy 模型与 repository
│   │   │   ├── models/
│   │   │   │   └── knowledge.py               # kg_nodes / kg_edges / evidence
│   │   │   └── repositories/
│   │   │       ├── knowledge_repository_impl.py
│   │   │       └── knowledge_source_projection_repository_impl.py
│   │   ├── tools/                             # 工具服务
│   │   │   ├── ocr_service.py
│   │   │   ├── browser_spy.py
│   │   │   ├── scheduler.py
│   │   │   └── skill_registry.py
│   │   └── tmux/                              # tmux 适配器
│   │       └── session.py                     # tmux 会话创建/读取/清理
│   │
│   └── common/                                # ── 通用模块 ──
│       ├── __init__.py
│       ├── types.py                           # 共享类型定义
│       └── exceptions.py                      # 统一异常定义
│
├── tests/                                     # 测试（与 src 平级）
│   ├── config.py
│   ├── test_fund.py
│   ├── test_market.py
│   └── ...
│
└── docs/                                      # 文档（与 src 平级）
    ├── 1. 项目概述/                              # 业务设计
    ├── 2. 架构设计/                              # 本文档所在
    └── 3. 实施方案/                              # 数据源/聚合/重构
```

---

## 5. 分层职责

### 接口层 (interfaces/)

外部接入点。**薄层**，只做：
1. 参数校验（FastAPI Pydantic model）
2. 调用应用层 AppService
3. 格式化响应

```python
# src/interfaces/api/routes/query.py
from src.application.services.query_service import QueryService

@router.get("/api/fund/{code}")
async def get_fund(code: str):
    return await QueryService.get_fund_snapshot(code)
```

**禁止**：直接操作 DB、直接调用客户端、包含业务逻辑。

知识图谱接口层还要额外遵守：

- `src/interfaces/api/routes/knowledge.py` 只定义 `/api/kg/...` 路由、请求模型和 HTTP 错误映射。
- `src/interfaces/cli/knowledge.py` 只读取命令行参数和文件，输出摘要或 JSON。
- API 和 CLI 都必须调用应用层 `KnowledgeService`，不能各自实现编译、检索、重建或质量扫描逻辑。
- 接口层不能直接 import `KnowledgeCompiler`、`FinancialKGAdapter`、`KnowledgeRepositoryImpl`。

### 应用层 (application/)

用例编排。协调多个领域和基础设施完成一个完整用例。

两种模式：

| | Services（用例编排） | Orchestrators（进程编排） |
|--|---------|---------|
| 调用方式 | `await svc.method()` → 返回 | 长驻运行，管理生命周期 |
| 状态 | 无状态 | 有状态（tmux 会话池、shutdown） |
| 代表 | AggregationService, TradeService, KnowledgeService | TaskOrchestrator |

```python
# src/application/services/aggregation_service.py
class AggregationService:
    @staticmethod
    async def query_news(since: str = None, category: str = None):
        """查询已聚合的新闻（读结果表）"""
        return db.query_ft_news(since=since, category=category)

    @staticmethod
    async def replay_news(start: str, end: str):
        """历史重放：从 raw_data 重新聚合"""
        records = raw_data.query_raw(domain="news", start=start, end=end)
        ...
```

知识图谱服务化后，应用层还承担两个明确职责：

| 模块 | 职责 |
|---|---|
| `src/application/dto/knowledge_dto.py` | 定义 API/CLI 共用的知识图谱用例输入输出 |
| `src/application/services/knowledge_adapter_registry.py` | 根据 `adapter_name` 创建领域 Adapter，避免接口层写死金融实现 |
| `src/application/services/knowledge_service.py` | 编排 compile、rebuild wiki、rebuild indexes、research context、quality scan |

应用层可以组装 repository 和 adapter，但不能泄露 FastAPI、click、SQLAlchemy session 到领域层。

### 领域层 (domain/)

**纯业务逻辑**。不依赖任何框架（不 import FastAPI、jettask、psycopg2）。

```python
# src/domain/aggregation/news.py
SOURCES = [
    ("cls", cls.get_telegraph_since, 180, normalize_cls),
    ...
]

async def run():
    """新闻聚合核心逻辑"""
    for name, fetch_fn, interval, normalize_fn in SOURCES:
        if not should_fetch(name, interval):
            continue
        try:
            raw = await fetch_fn(get_checkpoint(name))
            items = normalize_fn(raw)
            save(items)
            mark_fetched(name)
        except Exception as e:
            logger.warning(f"[{name}] 失败: {e}")
```

### 基础设施层 (infrastructure/)

技术实现，可替换。提供领域层和应用层所需的外部能力。

| 模块 | 技术 | 职责 |
|------|------|------|
| `clients/` | httpx, curl_cffi | 10 个外部数据源 API 客户端 |
| `db/` | psycopg2 | PostgreSQL CRUD + 建表 |
| `persistence/` | SQLAlchemy | ORM 模型与 repository，知识图谱事实库优先放这里 |
| `tools/` | httpx, subprocess | OCR、浏览器、APScheduler |
| `tmux/` | subprocess | tmux 会话管理（从 task_executor 拆出） |

---

## 6. 数据流

### 聚合数据流（jettask task 驱动）

```
jettask Schedule (定时触发)
    ↓
src/interfaces/tasks/collection_tasks.py      ← task 入口（薄层）
    ↓ await
src/domain/aggregation/news.py                ← 业务逻辑（遍历源、转换、去重）
    ↓ 调用                                      ↓ 写入
src/infrastructure/clients/cls.py             src/infrastructure/db/fund_db.py
src/infrastructure/clients/eastmoney.py           → ft_news 表
    ...
```

### API 查询数据流

```
HTTP GET /api/fund/110022
    ↓
src/interfaces/api/routes/query.py            ← 参数校验
    ↓ 调用
src/application/services/query_service.py     ← 用例编排（组合多个查询）
    ↓ 调用
src/infrastructure/clients/ths.py             ← 获取基金数据
src/infrastructure/db/fund_db.py              ← 查缓存
    ↓
返回 JSON
```

### 知识图谱编译与查询数据流

```
HTTP POST /api/kg/compile 或 CLI kg compile
    ↓
src/interfaces/api/routes/knowledge.py
src/interfaces/cli/knowledge.py               ← 参数校验，禁止直接写 DB
    ↓ 调用
src/application/dto/knowledge_dto.py          ← API/CLI 共用输入输出
    ↓
src/application/services/knowledge_service.py ← 编排投影、adapter、compiler、repository
src/application/services/knowledge_source_projection_service.py ← 读取 Raw Row 并投影为 Source Record
src/application/services/knowledge_adapter_registry.py
    ↓ 调用
src/domain/knowledge/repositories/knowledge_source_projection_repository.py ← Raw Row 读取协议
src/domain/knowledge_adapters/financial/source_projection.py               ← Source Record 生成
    ↓
src/domain/knowledge/compiler.py              ← 通用编译规则：节点/边/证据/版本
src/domain/knowledge_adapters/financial/      ← 金融领域规则
    ↓ 持久化
src/infrastructure/persistence/repositories/knowledge_repository_impl.py
src/infrastructure/persistence/repositories/knowledge_source_projection_repository_impl.py
    ↓
kg_nodes / kg_edges / kg_evidence / kg_versions

HTTP POST /api/kg/research-context 或 CLI kg query
    ↓
src/application/services/knowledge_service.py
    ↓
src/domain/knowledge/retrieval.py             ← RRF、上下文预算、结果协议
    ↓ 读取
knowledge_repository_impl.py / FTS / pgvector / graph index
```

知识图谱 API 不暴露表级 CRUD。外部只能调用用例级接口：

```text
compile
rebuild-wiki
rebuild-indexes
research-context
quality-scan
reviews
```

### 任务执行数据流

```
HTTP POST /api/task
    ↓
src/interfaces/api/routes/task.py             ← 参数校验
    ↓ 调用
src/application/services/task_service.py      ← 创建任务记录
    ↓ 委托
src/application/orchestrators/task_orchestrator.py  ← 管理 tmux 会话
    ↓ 使用
src/infrastructure/tmux/session.py            ← 创建/读取 tmux
src/infrastructure/db/task_db.py              ← 持久化任务状态
```

---

## 7. task_executor 拆分方案

当前 `services/task_executor.py`（600+ 行）是最大的技术债。拆分为三部分：

| 拆分后 | 层 | 职责 | 行数估计 |
|--------|---|------|---------|
| `src/application/orchestrators/task_orchestrator.py` | 应用层 | 任务生命周期：创建→执行→轮询→完成→清理 | ~200 |
| `src/infrastructure/tmux/session.py` | 基础设施 | tmux 会话：创建、发送命令、读取输出、清理 | ~150 |
| `src/domain/task/models.py` | 领域层 | 任务状态模型、同步任务类型判断 | ~50 |

原 `task_executor.py` 中的状态管理（`_active_tasks`、`_session_models`、`SESSION_IDLE_TIMEOUT`）归入 Orchestrator。
tmux 操作（`_create_session`、`_send_to_tmux`、`_read_output`）归入基础设施。

---

## 8. 实施方案

详见 [实施方案/](./实施方案/) 目录，分 4 步渐进式迁移：

| 步骤 | 内容 | 性质 | 风险 | 详情 |
|------|------|------|------|------|
| Step 1 | 项目初始化 | pyproject.toml + .env + 建骨架 | 无 | [01-项目初始化.md](./实施方案/01-项目初始化.md) |
| Step 2 | 代码平移 | 按层移文件 + 改 import | 低 | [02-代码平移.md](./实施方案/02-代码平移.md) |
| Step 3 | task_executor 拆分 | 拆为 orchestrator + tmux + models | 中 | [03-task_executor拆分.md](./实施方案/03-task_executor拆分.md) |
| Step 4 | 应用层与接口层重构 | AppService + router 瘦化 + common + middleware | 中 | [04-应用层与接口层重构.md](./实施方案/04-应用层与接口层重构.md) |

**原则**：Step 1-2 不改逻辑只搬代码，Step 3-4 做真正的重构。每步完成后系统可运行。

---

## 9. 与 jettask_v2 的对比

| 维度 | jettask_v2 | smart-fund-server |
|------|-----------|-------------------|
| 类型 | 通用框架 | 业务应用 |
| DI | dependency-injector 容器 | 简单 import（暂不引入 DI 框架） |
| 配置 | YAML + env + CLI → DI 注入 | config.py + env（够用） |
| 领域数 | 3（Worker/Task/Scheduler） | 5（Aggregation/Knowledge/Trading/Task/Query） |
| 协议 | 跨语言 Wire Protocol | 不需要（纯 Python 应用） |
| 事件总线 | Redis Stream 事件驱动 | 暂不需要（领域间直接调用即可） |

**设计取舍**：
- **不引入 DI 框架**：项目规模不大，简单 import 即可，避免过度工程
- **不引入事件总线**：领域间调用关系简单，直接调用比异步事件更易调试
- **不全局强制 Repository 抽象**：简单 CRUD 可继续使用现有 db 模块；跨表一致性、事实库、审计和版本治理这类复杂持久化使用 repository
- **保留 config.py 模式**：比 YAML + DI 注入更简单直接

---

## 10. 文件对照表

| 现有位置 | 目标位置 | 层 |
|---------|---------|---|
| `routers/` | `src/interfaces/api/routes/` | 接口 |
| `tasks/__init__.py` + `collection_tasks.py` | `src/interfaces/tasks/` | 接口 |
| `tasks/register_schedules.py` | `src/interfaces/cli/` | 接口（CLI） |
| `services/handlers/` | `src/domain/task/handlers/` | 领域 |
| `services/aggregators/` | `src/domain/aggregation/` | 领域 |
| 新增知识图谱通用层 | `src/domain/knowledge/` + `src/application/services/knowledge_service.py` + `src/application/dto/knowledge_dto.py` + `src/infrastructure/persistence/` | 领域+应用+基础设施 |
| 新增知识图谱对外服务 | `src/interfaces/api/routes/knowledge.py` + `src/interfaces/cli/knowledge.py` + `src/application/services/knowledge_adapter_registry.py` | 接口+应用 |
| 金融领域适配器 | `src/domain/knowledge_adapters/financial/` | 领域 |
| `services/trade/` | `src/domain/trading/` | 领域 |
| `services/task_executor.py` | 拆分 → `src/application/orchestrators/` + `src/infrastructure/tmux/` + `src/domain/task/` | 应用+基础设施+领域 |
| `services/clients/` | `src/infrastructure/clients/` | 基础设施 |
| `services/db/` | `src/infrastructure/db/` | 基础设施 |
| `services/tools/` | `src/infrastructure/tools/` | 基础设施 |
| `config.py` | `src/infrastructure/config/settings.py` | 基础设施 |
| `main.py` | `main.py`（不动，import src） | 入口 |

---

## 11. 与业务实施方案的对应关系

[业务架构](./业务架构.md) 定义了四个业务层：**客户端层 → 聚合层 → 知识层 → 业务层**。重构不改变业务分层，只是将代码按职责重新组织到 `src/` 的四层结构中。

### 业务层 → 代码层 映射

```
业务实施方案                          代码架构（src/）
─────────────                        ─────────────

                                     interfaces/
                                       api/routes/        ← API 入口
                                       tasks/             ← jettask task 入口
                                         ↓ 调用
                                     application/
                                       services/          ← 用例编排
                                         ↓ 调用
┌─ 业务层 ──────────────┐            domain/
│  决策引擎 / 风控 / 策略  │  ──→        trading/           ← 风控、指标、决策复盘
│  投研问答 / 事件消费     │             query_service.py   ← 查询编排
└───────────────────────┘               ↓ 读取语义上下文
┌─ 知识层 ──────────────┐            domain/
│  Compiler / KG-RAG     │  ──→        knowledge/          ← 节点、边、证据、检索
│  Wiki / 质量治理        │              ↓ 持久化
└───────────────────────┘               ↓
                                     infrastructure/
                                       persistence/       ← kg_* 事实库
                                         ↓
┌─ 聚合层 ──────────────┐            domain/
│  6 个聚合服务           │  ──→        aggregation/        ← 采集、转换、去重、入库
│  新闻/资金流/宏观/...    │              ↓ 调用
└───────────────────────┘               ↓
                                     infrastructure/
┌─ 客户端层 ─────────────┐              clients/           ← 10 个外部 API 客户端
│  9 个客户端 + 缓存       │  ──→        db/                ← PostgreSQL + ft_raw_data
│  + ft_raw_data 原始归档  │              tools/             ← OCR、浏览器等
└───────────────────────┘              config/             ← 统一配置
```

### 实施方案文档 → 代码位置

| 业务文档 | 描述 | 当前代码位置 | 重构后位置 |
|---------|------|------------|-----------|
| [1. 数据源对接/](../3.%20实施方案/1.%20数据源对接/) | 9 个客户端 API 对接 | `services/clients/` | `src/infrastructure/clients/` |
| [2. 数据聚合/](../3.%20实施方案/2.%20数据聚合/) | 6 个聚合 task + service | `services/aggregators/` + `tasks/` | `src/domain/aggregation/` + `src/interfaces/tasks/` |
| [4. 知识图谱/](../3.%20实施方案/4.%20知识图谱/) | 通用知识基础设施与领域 Adapter | — | `src/domain/knowledge/` + `src/application/services/knowledge_service.py` + `src/infrastructure/persistence/` |
| [业务架构](./业务架构.md) | 业务分层总览 | — | 本文档是其代码层面的细化 |
| AI事件抽取 | 数据处理层，消费知识层 | — | `src/domain/extraction/` + `src/application/services/` |
| 草稿/事件反馈回填 | 事件反馈聚合 | `services/aggregators/event_feedback.py` | `src/domain/aggregation/event_feedback.py` |

### 两份架构文档的关系

| | [业务架构](./业务架构.md) | 本文档（系统架构） |
|--|-------|-------|
| 视角 | **业务视角**：数据怎么流转 | **工程视角**：代码怎么组织 |
| 分层 | 客户端层 → 聚合层 → 知识层 → 业务层 | interfaces → application → domain → infrastructure |
| 关注点 | 采集什么数据、怎么聚合、怎么决策 | 文件放哪里、谁调谁、怎么迁移 |
| 稳定性 | 业务不变则不动 | 随项目规模演进 |

**重构不改变业务架构**：客户端层依然对接外部 API + 透明缓存 + ft_raw_data 归档，聚合层依然是每维度一个 jettask task 串行遍历源。改变的只是代码的目录组织方式。
