# 情绪舆情聚合

**Service：** `src/domain/aggregation/sentiment.py`
**Task 入口：** `src/interfaces/tasks/aggregator_tasks.py` → `agg_sentiment`（队列 `agg_sentiment`，Schedule 每 15 分钟）
**优先级：** P1 — 风控辅助
**目标表：** ft_sentiment
**状态：** ✅ 已上线（5 个源已入库）

---

## 职责

聚合股吧热度、雪球话题/热股、涨停统计、热门股排行等情绪指标，统一格式写入 `ft_sentiment`。

情绪数据反映市场"温度"。板块被过度讨论、情绪过热时是风险信号而非机会。

---

## 数据源

### 当前已接入的源（6 个）

| 源名 | 客户端 | 方法 | 间隔 | data_type | 状态 |
|------|--------|------|------|----------|------|
| `guba_popularity` | EM | `get_guba_popularity` | 30 分钟 | guba_popularity | ✅ |
| `limit_pool_up` | THS | `get_limit_pool("up")` | 3 小时 | limit_pool | ✅ |
| `limit_pool_down` | THS | `get_limit_pool("down")` | 3 小时 | limit_pool | ✅ |
| `xueqiu_hot_topics` | Xueqiu | `get_hot_topics` | 30 分钟 | xueqiu_hot_topics | ✅ |
| `xueqiu_hot_stocks` | Xueqiu | `get_hot_stocks` | 30 分钟 | xueqiu_hot_stocks | ✅ |
| `tencent_hot_stocks` | Tencent | `get_hot_stocks` | 30 分钟 | tencent_hot_stocks | ⚠️ 经常返回空 |

### 🔜 规划但未接入的源

| 数据 | 客户端 | 方法 | 备注 |
|------|--------|------|------|
| 个股股吧帖子 | EM | `get_guba_posts` / `get_guba_posts_since` | **重要**，应补齐 |
| 问财选股 | THS | `get_iwencai_query` | 按需调用（非定时） |
| 个股所属板块 | Tencent | `get_stock_plates` | 按需调用（非定时） |

### ⚠️ 已知问题

1. **guba_posts 未接入**：文档要求采集个股股吧帖子（反映具体个股的舆情），但当前 `_init_sources()` 没有这个源。
2. **tencent_hot_stocks 经常空数据**：某些时段 API 返回空 dict，已由 normalize 函数过滤，不会污染 DB。

---

## 统一输出格式

```python
{
    "data_type": str,       # guba_popularity/guba_posts/limit_pool/xueqiu_hot_topics/...
    "trade_date": str,      # YYYY-MM-DD
    "data": dict | list,    # JSONB，结构因 data_type 而异
    "created_at": str,      # ISO 8601（入库时间）
}
```

**空数据过滤**：`_save()` 和各 normalize 函数会自动过滤 `data` 为空（空 dict / 空 list / None）的记录，避免入库垃圾数据。例如 tencent_hot_stocks 返回 `{"5min":[],"1hour":[]}` 时会被识别为"无内容"并跳过。

### data_type 枚举

| data_type | 数据内容 |
|-----------|---------|
| `guba_popularity` | 股吧人气排行榜 |
| `guba_posts` | 个股股吧帖子列表 |
| `limit_pool` | 涨停/跌停统计 + 涨停原因分布 |
| `xueqiu_hot_topics` | 雪球热门话题 |
| `xueqiu_hot_stocks` | 雪球热股排行 |
| `tencent_hot_stocks` | 腾讯热门股（5分/1时/1日/1周） |

---

## 情绪信号

| 信号 | 判断逻辑 | 用途 |
|------|---------|------|
| 板块过热 | 雪球话题讨论数 > 200 + 股吧相关帖子激增 | 降低该板块事件权重 |
| 涨停/跌停比 | 涨停数 / (涨停数 + 跌停数) | 市场整体情绪温度 |
| 涨停原因集中 | 某原因涨停数 > 总涨停 30% | 当日市场主线 |
| 热股重叠 | 雪球热股 ∩ 腾讯热股 | 高关注度个股（可能过热） |

---

## 核心函数

```python
# src/domain/aggregation/sentiment.py

async def run():
    """task 入口：串行采集股吧/涨停/社交/热股数据"""

async def fetch_guba(codes: list[str] | None = None) -> list[dict]:
    """获取股吧数据（人气 + 帖子）"""

async def fetch_limit_pool() -> dict:
    """获取涨停/跌停统计"""

async def fetch_social() -> list[dict]:
    """获取社交媒体数据（雪球话题 + 热股）"""

async def fetch_hot_stocks() -> dict:
    """获取热门股排行（整合雪球 + 腾讯，去重）"""

async def get_market_temperature() -> dict:
    """计算市场情绪温度（综合多指标）
    返回: {"temperature": 0-100, "level": "cold/warm/hot/extreme", "signals": [...]}
    """

def save(items: list[dict]) -> int:
    """写入 ft_sentiment"""
```

---

## 建表 DDL

```sql
CREATE TABLE IF NOT EXISTS ft_sentiment (
    id          SERIAL PRIMARY KEY,
    data_type   VARCHAR(32) NOT NULL,
    trade_date  DATE NOT NULL,
    data        JSONB NOT NULL,
    created_at  TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_ft_sentiment_type_date ON ft_sentiment(data_type, trade_date);
```

> 历史备注：字段 `captured_at` 已于 2026-04-11 重命名为 `created_at`，与其他聚合表保持一致。
