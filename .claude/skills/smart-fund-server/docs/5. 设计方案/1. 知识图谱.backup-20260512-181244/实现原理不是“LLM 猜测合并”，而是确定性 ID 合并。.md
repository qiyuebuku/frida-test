实现原理不是“LLM 猜测合并”，而是确定性 ID 合并。

这里的确定性id是怎么来的？问题是一个新闻事件 可能跨越的时间线很长，他总能生成相同的id嘛？

他的名称可能不会完全一样，因为名称可能会有细微的差距啊

我们现在采集的原始数据都已经在表里了，所以我理解baseline_records 的输入是不是就是我们表里的数据呢？表里的数据都是非结构化的 我们如何转换？我理解将非结构化数据改为可以被知识图谱输入的数据也是 知识图谱应该有的能力吧  /home/yuyang/frida-test/.claude/skills/smart-fund-server/docs/5. 设计方案/1. 知识图谱/1. 知识图谱总体设计方案.md当初的设计方案是怎样的啊？


现在先帮我将  ft_news 的 projection 的真实数据接入进来 我先看看效果如何再做决定