1. 我们这个方案使用那种方式进行更新呢？同步还是异步？我更加倾向于使用异步任务建立这个高级认知报告 -- 这里后来证明是异步的
2. 只根据有效 Edge 形成 Card 关系连通子图 -- 这里的评定标准是什么？
3. 根据受影响关系子图增量创建 -- 如何增量呢？
4. 在事实报告之后独立生成条件性预测，并与事实图严格隔离 -- 如何隔离呢？
5. 我们这个方案的意义就是完全取代旧的方案 也就是说旧的方案可以删除了

6. 3.1 原文核验正关系 -- 我有一个疑问，那就是现在的关系确认后 是否会创建 edge呢？我们现在有edge表嘛？如果没有的话 那第三阶段是进行不下去的

7. 我们的llm请求层现在应该是公共方法吧，那么我觉得应该建立一个表，专门记录所有请求的元数据，例如请求的参数模型返回值等等，总之跟模型所有有关的请求都记录下来，后续我们的数据分析还有模型训练可以用到，再就是模型返回的 费用信息也要返回记录，用于后续的成本分析

adapter_name -- 都有哪些呢？

decision_class 和 relation_kind 是不是有重叠呢？

invalidated_at -- 改为 delete_at会不会好点？



kg_relation_evidence_verify -- 我观察到一个奇怪的事情，那就是有些no_relation 的内容，他也会输出大量的内容，我感觉如果没有关系根本无需输出，再就是既然都没有关系了，但是还在输出：source_evidence_refs 和 target_evidence_refs？


adapter_name --可以去掉了，暂时没有想好如何用它