# Novel-KB 架构简化方案讨论

> 日期：2026-02-12
> 状态：**讨论中**

---

## 一、当前架构问题分析

### 1.1 依赖关系图

```
原文 (text/)
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│  T2 (batch_summary.py)                                       │
│  产出: plot/chapters/chXXXX.md                               │
└─────────────────────────┬───────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
┌─────────────────┐ ┌───────────┐ ┌─────────────────────────────┐
│ T3 剧情层        │ │ T6 读者层 │ │ T4 角色层                    │
│ batch_plot.py   │ │           │ │ batch_character.py          │
│ 依赖: T2        │ │ 依赖: T2  │ │ 依赖: T2 + T3               │
└────────┬────────┘ └───────────┘ └──────────────┬──────────────┘
         │                                      │
         └──────────────────┬───────────────────┘
                            ▼
              ┌─────────────────────────────────┐
              │ T5 世界层                        │
              │ batch_world.py                  │
              │ 依赖: T2 + T3 + T4              │
              └─────────────────────────────────┘

T7 风格层 (batch_style.py) 依赖: T1 原文
```

### 1.2 核心问题

| 问题 | 描述 | 影响 |
|------|------|------|
| **依赖链过长** | T5 依赖 T2+T3+T4，用户需按顺序运行 4 个阶段 | 使用门槛高 |
| **重复读取** | T3/T4/T5 都要读取 T2 的 1000+ 个章节摘要 | IO 开销大 |
| **阶段拆分过细** | 7 个阶段，每个阶段又有 2-4 个子步骤 | 流程复杂 |
| **上下文不共享** | 每个阶段独立进程，无共享内存 | 无法利用缓存 |
| **增量更新困难** | 新增章节需要重跑整个流程 | 维护成本高 |

### 1.3 数据读取统计

假设 1200 章的小说，全量构建时：

| 阶段 | 读取文件 | 重复读取 |
|------|----------|----------|
| T2 | 1200 个原文文件 | - |
| T3 | 1200 个摘要文件 | 1200 |
| T4 | 1200 个摘要 + 6 个弧文件 | 1200+ |
| T5 | 1200 个摘要 + 6 个弧文件 + 15 个角色档案 | 1200+ |
| T6 | 1200 个摘要（提取评论情绪） | 1200 |
| **总计** | - | **4800+ 次重复读取** |

---

## 二、简化方案对比

### 方案 A：阶段合并（激进）

**思路**：将 T2+T3+T4+T5 合并为一个"内容提取"阶段

```
原文 → [统一提取器] → 摘要 + 剧情 + 角色 + 世界观
```

**优点**：
- 一次读取，无重复
- 流程最简单
- AI 可以综合理解上下文

**缺点**：
- 单次 AI 调用上下文太大（1200 章 × 3KB = 3.6MB）
- 失败后需要全部重跑
- 无法单独重跑某个层

**适用场景**：新书首次构建

---

### 方案 B：共享预处理层（渐进）

**思路**：增加预处理层，一次性读取所有数据，生成标准化的中间格式

```
原文 → [预处理] → unified_data.json
                      │
          ┌───────────┼───────────┐
          ▼           ▼           ▼
       [T3]        [T4]        [T5]
       剧情层      角色层      世界层
```

**中间格式** (`unified_data.json`)：
```json
{
  "chapters": [
    {
      "ch": "ch0001",
      "text": "原文内容...",
      "summary": {
        "characters": [...],
        "settings": [...],
        "items": [...],
        "plot_beats": [...]
      }
    }
  ],
  "arcs": [...],
  "stats": {...}
}
```

**优点**：
- 保留阶段独立性
- 大幅减少重复读取
- 支持增量更新（只处理新增章节）

**缺点**：
- 需要设计新的中间格式
- 增加存储空间（~2x）

---

### 方案 C：内存流水线

**思路**：在内存中传递数据，不落盘中间结果

```python
# 伪代码
chapters = load_all_chapters()  # 一次读取
summaries = batch_summarize(chapters)  # T2
arcs = extract_arcs(summaries)  # T3
characters = extract_characters(summaries, arcs)  # T4
world = extract_world(summaries, arcs, characters)  # T5
save_all(summaries, arcs, characters, world)  # 一次写入
```

**优点**：
- 无 IO 重复
- 流程清晰
- 代码简洁

**缺点**：
- 需要大内存（1200 章 × 10KB = 12MB，可接受）
- 失败后需要全部重跑
- 无法断点续传

---

### 方案 D：Agent Teams 蜂群模式

**思路**：利用 Agent Teams 创建并行队友，共享任务列表

```
┌─────────────────────────────────────────────────────────┐
│                    Lead Agent                            │
│  - 创建团队                                              │
│  - 分配任务（按章节范围）                                 │
│  - 收集结果                                              │
└───────────────────────┬─────────────────────────────────┘
                        │
┌───────────────────────┼─────────────────────────────────┐
│                   Agent Team                             │
├───────────────────────┼─────────────────────┬───────────┤
│  Teammate 1           │  Teammate 2         │  Teammate N│
│  ch001-100            │  ch101-200          │  ...       │
│  ┌─────────────────┐  │  ┌─────────────────┐│            │
│  │ T2: 摘要        │  │  │ T2: 摘要        ││            │
│  │ T3: 剧情片段    │  │  │ T3: 剧情片段    ││            │
│  │ T4: 角色提及    │  │  │ T4: 角色提及    ││            │
│  │ T5: 世界设定    │  │  │ T5: 世界设定    ││            │
│  └─────────────────┘  │  └─────────────────┘│            │
└───────────────────────┴─────────────────────┴───────────┘
                        │
                        ▼
              [汇总合并] → 最终知识库
```

**优点**：
- 天然并行
- 队友间可通信协调
- 支持 Agent Teams 的 Hooks 质量门控
- 更适合大规模任务

**缺点**：
- 需要 Agent Teams 支持（实验性功能）
- 汇总合并逻辑复杂
- Token 成本较高

---

## 三、推荐方案

### 3.1 核心思路：合并提取 + 分离合并

**问题根源**：当前 T2/T3/T4/T5 都要读取相同的数据（原文/摘要），造成重复 IO

**解决方案**：
1. **合并提取**：1 次 AI 调用同时提取摘要+剧情+角色+设定
2. **分离合并**：全局合并作为独立阶段，只运行 1 次

```
┌─────────────────────────────────────────────────────────────┐
│           阶段 1：统一提取（按章节并行，可蜂群）              │
│                                                             │
│  原文 ──→ [统一提取器] ──→ 摘要 + 剧情点 + 角色 + 设定       │
│           (1次AI/章)                                        │
│                    ↓                                        │
│           保存到 .cache/chXXXX_extracted.json               │
│                                                             │
│  每章读取原文 1 次，AI 调用 1 次                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼ 1200 个提取结果文件
┌─────────────────────────────────────────────────────────────┐
│           阶段 2：全局合并（只运行 1 次）                     │
│                                                             │
│  读取所有 chXXXX_extracted.json                              │
│        ↓                                                    │
│  合并剧情点 ──→ 弧 + 主线 + 伏笔                             │
│  合并角色 ──→ 角色档案 + 关系网                              │
│  合并设定 ──→ 力量/地理/势力/规则                            │
│        ↓                                                    │
│  保存到最终目录 (plot/, characters/, world/)                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 对比分析

| 对比项 | 旧方案 | 新方案 |
|--------|--------|--------|
| **原文读取** | T2 读取 1 次 | 统一提取读取 1 次 |
| **摘要读取** | T3+T4+T5 各读 1 次（共 3 次） | **0 次**（内存传递） |
| **AI 调用/章** | 3-4 次（T2+T3+T4+T5） | **1 次** |
| **总 AI 调用** | 1200 × 4 = 4800 次 | 1200 × 1 + 1 = **1201 次** |
| **全局处理** | 分散在各阶段 | 独立合并阶段 |

### 3.3 统一提取的 Prompt 设计

```markdown
你是小说分析专家。请分析以下章节内容，一次性提取：

## 输出格式（JSON）

{
  "summary": {
    "main_events": ["事件1", "事件2"],
    "new_settings": [{"name": "设定名", "desc": "描述"}],
    "important_items": [{"name": "物品名", "desc": "描述"}]
  },
  "plot": {
    "beats": [{"type": "转折/伏笔/高潮", "content": "内容"}],
    "emotion_curve": "上升/平稳/下降"
  },
  "characters": [
    {"name": "角色名", "action": "行为", "state_change": "状态变化"}
  ],
  "world": {
    "power_system": [...],
    "geography": [...],
    "factions": [...],
    "rules": [...]
  }
}
```

### 3.4 中间产物结构

```
.cache/
├── ch0001_extracted.json   # 每章的提取结果
├── ch0002_extracted.json
├── ...
└── merge_progress.json     # 合并进度
```

### 3.5 最终目录结构（保持不变）

```
plot/
├── chapters/chXXXX.md      # 从 summary 生成
├── outline/arc_XX.md       # 从 plot.beats 合并
└── open_loops.md           # 从 plot.beats 合并

characters/
├── {name}.md               # 从 characters 合并
└── relationships.md        # 从 characters 合并

world/
├── power_system/           # 从 world.power_system 合并
├── geography/              # 从 world.geography 合并
├── factions/               # 从 world.factions 合并
└── rules/                  # 从 world.rules 合并
```

### 3.6 实施步骤

1. **新增 `unified_extract.py`**：
   - 合并 T2+T3+T4+T5 的提取逻辑
   - 1 次 AI 调用提取所有信息
   - 保存到 `.cache/chXXXX_extracted.json`

2. **新增 `global_merge.py`**：
   - 读取所有提取结果
   - 合并剧情弧、角色档案、世界设定
   - 保存到最终目录

3. **保留独立入口**（调试用）：
   - `--extract-only`: 只运行提取
   - `--merge-only`: 只运行合并
   - `--phase all`: 完整流程

### 3.2 长期：方案 D Agent Teams

当 Agent Teams 功能稳定后，可以进一步升级为蜂群模式：

```bash
# 一键全流程（蜂群模式）
/novel-kb-unified /path/to/kb --phase all --concurrency 8

# 8 个队友并行处理
# 每个队友负责 150 章
# 每个队友同时完成 T2+T3+T4+T5
# Lead Agent 汇总合并
```

---

## 四、新的目录结构

### 4.1 简化后的目录

```
novel_kb/{book_name}/
├── .cache/                     # 新增：预处理缓存
│   ├── chapters_index.json     # 章节索引（轻量，~120KB）
│   └── .pipeline_progress.json # 流水线进度
│
├── plot/                       # 剧情层（保持不变）
├── characters/                 # 角色层（保持不变）
├── world/                      # 世界层（保持不变）
├── reader/                     # 读者层（保持不变）
├── style/                      # 风格层（保持不变）
├── text/                       # 文本层（保持不变）
└── drafts/                     # 草稿区（保持不变）
```

### 4.2 chapters_index.json 格式（轻量）

```json
{
  "meta": {
    "total_chapters": 1201,
    "last_updated": "2026-02-12T18:00:00Z",
    "version": "1.0"
  },
  "chapters": [
    {
      "id": "ch0001",
      "path": "text/ch0001.md",
      "words": 3500,
      "hash": "sha256:abc123...",
      "processed": true,
      "processed_at": "2026-02-10T10:00:00Z"
    },
    {
      "id": "ch0002",
      "path": "text/ch0002.md",
      "words": 3200,
      "hash": "sha256:def456...",
      "processed": true,
      "processed_at": "2026-02-10T10:05:00Z"
    }
    // ... 只有元信息，不包含原文内容
  ]
}
```

**文件大小估算**：
- 1201 章 × 150 字节/条目 ≈ **180KB**
- 可直接打开编辑，Git diff 友好

### 4.3 内存使用对比

| 场景 | 旧方案 | 新方案 |
|------|--------|--------|
| 预处理 | 逐个读取，无缓存 | 生成轻量索引 |
| T2 处理 | 逐个读取原文 | 按需读取单章原文 |
| T3/T4/T5 | 重新读取摘要文件 | **内存传递**，不重复读取 |
| 内存峰值 | 不累积 | **1章原文 + 1章摘要** ≈ 5KB
```

---

## 五、改造工作量估算

| 任务 | 工作量 | 优先级 |
|------|--------|--------|
| 设计统一提取 Prompt | 2h | P0 |
| 新增 `unified_extract.py` | 4h | P0 |
| 新增 `global_merge.py` | 3h | P0 |
| 更新 `SKILL.md` | 1h | P1 |
| 更新 `kb_orchestrator.py` | 1h | P1 |
| 测试验证 | 2h | P1 |
| **总计** | **13h** | - |

### 新增文件

```
.claude/skills/novel-kb/
├── unified_extract.py     # 新增：统一提取（替代 T2+T3+T4+T5 的提取部分）
├── global_merge.py        # 新增：全局合并
├── prompts/
│   └── unified_extract.md # 新增：统一提取 Prompt
├── batch_summary.py       # 保留（调试用）
├── batch_plot.py          # 保留（调试用）
├── batch_character.py     # 保留（调试用）
├── batch_world.py         # 保留（调试用）
└── ...
```

### 收益预估

| 指标 | 旧方案 | 新方案 | 提升 |
|------|--------|--------|------|
| AI 调用次数 | 4800 次 | 1201 次 | **75% 减少** |
| IO 读取次数 | 4800+ 次 | 1200 次 | **75% 减少** |
| 用户操作步骤 | 4 个阶段 | 1 个命令 | **75% 简化** |

---

## 六、讨论问题

1. **统一提取的粒度**：
   - 方案 A：1 次 AI 调用提取摘要+剧情+角色+设定（推荐）
   - 方案 B：2 次调用（先摘要，再从摘要提取其他）
   - 方案 A 上下文更大但效率更高，你倾向哪个？

2. **合并策略**：
   - 全量合并（每次重跑全部 1200 章）
   - 增量合并（只合并新增章节的结果）
   - 增量合并更复杂但更适合长期维护

3. **Prompt 设计**：
   - 统一提取的 Prompt 需要覆盖所有层的信息
   - 是否需要分类型的 Prompt（如仙侠类、都市类）？

4. **Agent Teams 集成**：
   - 统一提取阶段可以用蜂群模式并行
   - 每个队友处理 100-200 章
   - 是否立即支持？

5. **向后兼容**：
   - 保留旧的 batch_*.py 入口（调试用）
   - 新增 unified_extract.py 作为默认入口
   - 你觉得这样可以吗？

---

## 七、下一步

1. 确认方案选择
2. 设计统一提取的 Prompt
3. 实现 unified_extract.py
4. 实现 global_merge.py
5. 测试验证
6. 集成到 SKILL.md
