# 日志同步与一键启动

> 日期：2026-02-07
> 状态：**已完成** — RPC 轮询日志 + 一键启动脚本

---

## 一、方案概述

### 通信链路

```
Android App → 日志写入内存缓冲区（logBuffer, 最大 5000 条）
                              ↑ 自动，无需网络
WSL2 log_poller.py (127.0.0.1:12345) → ADB forward → RPC getLogs → 增量拉取 → 写入 logs/qdhook.log
```

WSL2 与 Windows 网络已打通，`adb forward` 端口可从 WSL2 直接访问 `127.0.0.1:12345`，无需任何中间桥接。

**优势：**
- 不需要额外 TCP 服务和 `adb reverse`
- 复用已有的 RPC 通道（`localabstract:qdhook_rpc`）
- 日志存在内存缓冲区，poller 断连不丢失（5000 条容量）
- 支持 `since` 增量拉取，不重复

---

## 二、关键组件

### 2.1 手机端：内存日志缓冲区

**文件**：`MainHook.java`

```java
// 缓冲区配置
private static final int MAX_LOG_BUFFER = 5000;
private static final LinkedList<String> logBuffer = new LinkedList<>();

// 所有日志统一写入缓冲区
private static void logRemote(String tag, String message) {
    String logJson = new JSONObject()
        .put("timestamp", System.currentTimeMillis())
        .put("tag", tag)
        .put("thread", Thread.currentThread().getName())
        .put("message", message)
        .toString();

    synchronized (logBuffer) {
        logBuffer.addLast(logJson);
        while (logBuffer.size() > MAX_LOG_BUFFER) {
            logBuffer.removeFirst();
        }
    }
}
```

### 2.2 RPC 命令：getLogs

**请求**：
```json
{"cmd": "getLogs", "limit": 500, "since": 1770364989680, "tag": ""}
```

**参数**：
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| limit | int | 200 | 最多返回条数 |
| since | long | 0 | 只返回此时间戳之后的日志 |
| tag | string | "" | 过滤关键词（匹配 tag + message） |

**响应**：
```json
{
  "success": true,
  "count": 107,
  "total": 5000,
  "maxTimestamp": 1770365089342,
  "data": [
    {"timestamp": 1770364937269, "tag": "QDHook", "thread": "QDHook-Delay", "message": "=== 开始安装搜索 Hook ==="},
    ...
  ]
}
```

**关键设计决策**：日志按时间升序返回（最旧在前）。实现方式是先从缓冲区尾部向前收集，再反转后输出。

### 2.3 PC 端：log_poller.py

**文件**：`scripts/log_poller.py`

**运行环境**：WSL2 Python3（直连 ADB forward 端口）

**工作流程**：
1. 连接 `127.0.0.1:12345`（ADB forward 到手机 RPC）
2. 发送 `getLogs`，携带 `since` 参数实现增量拉取
3. 格式化日志行写入 `logs/qdhook.log`
4. 每 2 秒轮询一次
5. 固定文件名，每次启动覆盖写入

**日志输出格式**：
```
[2026-02-06 16:02:17.269] [QDHook-Delay] [QDHook] === 开始安装搜索 Hook ===
[2026-02-06 16:02:17.275] [DefaultDispatcher-worker-1] [QDProbe] NETWORK_CALL | enqueue | https://...
```

---

## 三、一键启动脚本

**文件**：`start.sh`（项目根目录）

```bash
./start.sh
```

**自动执行步骤**：

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1/5 | `am force-stop` | 强制停止 App |
| 2/5 | `monkey` 启动 | 避免 `am start` 的 -92 错误 |
| 3/5 | `sleep 18` | 等待 360 加固脱壳 + Hook 初始化 |
| 4/5 | `adb forward` | 打通 RPC 通道 |
| 5/5 | 启动 log_poller | 持续同步日志到 `logs/qdhook.log` |

脚本在步骤 4 后会验证 RPC 连接，失败则报错退出。

**输出示例**：
```
[1/5] 强制停止 com.qidian.QDReader ...
[2/5] 启动 App ...
[3/5] 等待 Hook 初始化 (18s) ...
[4/5] 设置 ADB forward ...
   RPC 连接成功，缓冲区已有 105 条日志
[5/5] 同步日志到 /home/yuyang/frida-test/qidian/logs/qdhook.log ...
   按 Ctrl+C 停止
```

---

## 四、踩坑记录

### 4.1 RPC JSON 紧凑格式（重要）

**问题**：`handleRpcCommand` 返回的 JSON 使用 `toString(1)` 格式化输出（便于调试），但 RPC 协议以 `\n` 作为消息分隔符。当响应是大 JSON（如章节列表，含数百个章节）时，格式化后的 JSON 包含换行符，客户端在第一个 `\n` 处就停止读取，导致 JSON 被截断。

**解决**：在 `handleClient()` 方法中，发送响应前将 JSON 转换为紧凑格式：
- 先尝试 `new JSONObject(response).toString()` 去除所有换行和缩进
- 失败时 fallback 为 `response.replace("\n", " ").replace("\r", "")`

**实现位置**：`MainHook.java:1641-1670` → `handleClient()` 方法

> **注意：此 fix 必须在 `handleClient()` 统一处理，而非每个 handler 单独处理。** 如果未来新增 RPC handler 返回格式化 JSON（`toString(1)`），`handleClient()` 会自动转换为紧凑格式，handler 不需要关心这个问题。但如果 handler 返回的不是合法 JSON（如纯文本错误消息），fallback 逻辑会用空格替换所有换行符。

### 4.2 getLogs 返回 0 字节

**现象**：`getStatus` 正常返回，但 `getLogs` 返回 0 字节。

**根因**：初版 `handleGetLogs` 使用 `LinkedList.get(i)` + 复杂索引逻辑，JSON 解析异常未被捕获导致方法静默崩溃。

**解决**：简化为 `ArrayList snapshot` + 快照拷贝 + 所有 JSON 操作包在 try-catch 内。

### 4.3 日志顺序反了

**现象**：日志文件中最新的在前面，不符合阅读习惯。

**根因**：`handleGetLogs` 从缓冲区尾部向前遍历，直接 `arr.put(obj)`。

**解决**：先收集到 `ArrayList<JSONObject> matched`，再从尾到头添加到 JSONArray，实现时间升序输出。

### 4.4 ADB forward 失效

**现象**：App 重启后 poller 报 "RPC connection failed"。

**原因**：`adb forward` 对应的 LocalSocket 在 App 进程死亡后失效，新进程的 socket 名称相同但 forward 映射需要重建。

**解决**：每次 App 重启后需重新执行 `adb forward`，`start.sh` 已自动处理。

---

## 五、文件清单

| 文件 | 作用 |
|------|------|
| `start.sh` | 一键启动脚本（项目根目录） |
| `scripts/log_poller.py` | RPC 日志轮询器（WSL2 Python3 运行） |
| `logs/qdhook.log` | 日志输出文件（固定文件名，覆盖写入） |

---

## 六、代码修改后重新部署

当修改 `MainHook.java` 后，需要重新编译和部署：

### 6.1 完整部署流程

```bash
# 1. 进入项目目录
cd /home/yuyang/frida-test/qidian

# 2. 清理并编译（使用 clean 确保代码更新）
./gradlew clean assembleDebug

# 3. 提取 DEX 文件（必须提取所有 classes*.dex）
unzip -o app/build/outputs/apk/debug/app-debug.apk 'classes*.dex' -d zygisk/extracted/

# 4. 部署到手机
ADB="/mnt/d/123pan/Downloads/一加Ace6/adb命令行/adb.exe -s 3B15BJ00GZL00000"

# 推送 DEX 文件到临时目录
$ADB push zygisk/extracted/classes.dex /data/local/tmp/
$ADB push zygisk/extracted/classes2.dex /data/local/tmp/
$ADB push zygisk/extracted/classes3.dex /data/local/tmp/

# 复制到模块目录（需要 root 权限）
$ADB shell "su -c 'cp /data/local/tmp/classes.dex /data/adb/modules/qdhook_zygisk/dex/'"
$ADB shell "su -c 'cp /data/local/tmp/classes2.dex /data/adb/modules/qdhook_zygisk/dex/'"
$ADB shell "su -c 'cp /data/local/tmp/classes3.dex /data/adb/modules/qdhook_zygisk/dex/'"

# 5. 重启起点 App
$ADB shell "am force-stop com.qidian.QDReader"
sleep 2
$ADB shell "monkey -p com.qidian.QDReader -c android.intent.category.LAUNCHER 1"
```

### 6.2 验证部署成功

```bash
cd /home/yuyang/frida-test/qidian/scripts

# 测试 RPC 连接
python qidian_api.py ping
# 预期输出: pong

# 查看 Hook 状态
python qidian_api.py status
# 预期输出包含: "rpcRunning": true
```

### 6.3 关键注意事项

| 项目 | 说明 |
|------|------|
| **clean 编译** | 必须使用 `./gradlew clean`，否则代码更改可能不会被打包 |
| **所有 DEX 文件** | 主代码在 `classes3.dex`，三个文件缺一不可 |
| **强制停止** | `am force-stop` 确保 App 完全退出，Zygisk 才会重新加载 DEX |
| **无需重启手机** | 只需重启 App 即可，Zygisk 会在下次进程启动时自动加载新 DEX |
| **ADB 路径** | `/mnt/d/123pan/Downloads/一加Ace6/adb命令行/adb.exe -s 3B15BJ00GZL00000` |

---

## 七、快速故障排查

### 7.1 RPC 连接失败

**症状**：`RPC 错误: RPC 连接被拒绝`

**排查**：
```bash
# 确认 App 进程存在
ADB="/mnt/d/123pan/Downloads/一加Ace6/adb命令行/adb.exe -s 3B15BJ00GZL00000"
$ADB shell "ps | grep com.qidian.QDReader"

# 确认端口转发
$ADB forward --list

# 重启 App
$ADB shell "am force-stop com.qidian.QDReader"
sleep 2
$ADB shell "monkey -p com.qidian.QDReader -c android.intent.category.LAUNCHER 1"
```

### 7.2 DEX 文件未生效

**症状**：代码修改后行为没有变化

**排查**：
1. 确认使用了 `./gradlew clean`
2. 检查三个 DEX 文件是否都被推送
3. 确认使用了 `am force-stop` 而非简单的 `am start`
4. 在 App 中手动操作触发 Hook，查看日志是否包含新的调试信息

### 7.3 日志输出异常

**症状**：`logs/qdhook.log` 没有输出或输出停止

**排查**：
```bash
# 检查 log_poller.py 是否运行
ps aux | grep log_poller

# 手动测试 RPC
cd /home/yuyang/frida-test/qidian/scripts
python qidian_api.py ping

# 查看 App 内日志数量
python qidian_api.py status
# 检查 logBufferCount 是否 > 0
```

---

## 八、实现历史

| 日期 | 修改内容 | 代码位置 |
|------|---------|---------|
| 2026-02-07 | 初始版本：RPC 日志轮询 + 一键启动脚本 | - |
| 2026-02-08 | 添加 HTTP 请求节流（30ms 最小间隔），防止评论采集时 RPC 超时 | `MainHook.java` 行 97-99, 4557-4569, 4590 |

---

*文档最后更新：2026-02-08*
