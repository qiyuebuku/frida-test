1. 还是召回50吧，这够了
2. rerank 12也够了


 现在一个 LLM 同时承担 Card 拆分 -- 这里我觉得可以使用追问模式，第一阶段承担card拆分，证据绑定


 而关系判断可以在他输出完成之后，使用完全相同的prompt前缀，追问关系判断，这样可以使用到 模型的前缀缓存，减少 token消耗




  真正占 Token 的主要是 System Prompt，不是这个层级。Untitled-3.json 有 36KB，是因为 Langfuse 同时记录了 Prompt、预览副本、Schema 和输出；prompt_text_preview 不会再次发给模型。

  但仍可进一步简化成行格式：

  published_at=2026-07-13T02:56:59+00:00
  <title>[s0001]标题
  [s0002]第一部分，[s0003]第二部分，[s0004]第三部分。
  [s0005]下一句……

  这样仍然具备：

  - 换行表示完整句子边界
  - [s0001] 表示细粒度证据坐标
  - <title> 区分标题
  - 模型可以跨多个 Ref 合并 Card

  这个样本会从 887 降到约 491 字符。还可以把 chunk_id 从 Prompt 删除，只保留在 metadata，因为模型根本不需要知道它，且相同正文能更容易复用缓存。

  所以结论是：层级表达有价值，但没有必要使用冗长 JSON；改成带 Ref 的逐行文本更合适。System Prompt 的重复规则才是下一步主要压缩目标。




python scripts/知识图谱/01_ft_news_card_relation_workflow.py \
--mode cards \
--limit 20 \
--concurrency 10 \
--chunk-timeout 240


1. Card 合法、Relation 非法：保留 Cards，只丢弃非法 Relation，不重试。 --- 我觉得是不是可以把