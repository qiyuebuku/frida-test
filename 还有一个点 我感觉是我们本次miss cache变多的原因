还有一个点 我感觉是我们本次miss cache变多的原因


那就是之前的候选数据都在一个上下文中：
seed_community_catalog 跟 candidate_communities 是一起的，那么假设后续新增了一部分候选内容，那么也只有新增的哪些会miss 因为前缀没有变化

但是现在他们两个分成了两部分，只要 seed_community_catalog 的数量进行变化 那么后续所有的缓存都会失效 也就是 candidate_communities 的内容会100%失效，之前还能缓存一部分的


而且现在的顺序是：
seed_community_catalog
"max_attach": 5,
candidate_communities


max_attach 是一直变化的 那么candidate_communities也会失效

因为 seed_community_catalog 和 candidate_communities 是公用一部分缓存 哪怕你把他们在字段上独立 仍然解决不了缓存问题 

之前是动态字段变化了缓存才失效，但是概率比较小 就是要好几次才会失效一次  现在只要每新增一条communit topic 就会100%失效，这是严重的问题


所以这么来看他还不如不拆分呢？


你这个方案太复杂了，如果我们每次请求都加上锁 让他变成串行的，然后每次请求的候选相比于前一次 都只新增不修改，每次都只增加上一波后续中没有的数据，然后我们把 seed_community_catalog 和
  candidate_communities 合并，然后如果这一次的动态内容变化了，再单独在后面声明下变化的内容，那么这样的话我们的提示词前缀永远都不会变换，然后我们每当提示词长度达到一定的长度 就进行一轮裁剪






  关于你说的的问题：
 1. 当前是“逐个 intent 裁决”，不是“先归并同类 intent，再建主题” -- 我们每次让llm裁决之前都会先将近似的主题通过rag召回来 作为上下文判断 是否要进行新建或者更新，他为啥还会创建出三个不同的主题呢？这说明是模型问题还是我们给的上下文不够呢？





   1. seed 被手动加入候选；
  2. RAG 召回又召回了相同 seed；


  我们加入候选不是只有一个途径 就是通过 rag召回再排序嘛？为啥还会有手动加入seed的机制呢？









  
  我想到了一个好的方法 可以极高的提升cache 命中率  那就是候选内容只新增不修改  每次召回+排序后的内容 都先判断是是否已经在之前的请求中存在了 然后把不存在的内容增加到候选的后面 然后我们也不存在什么动态内容了 因为后续假某一个主题的动态内容发生了变化 也就是 dynamic_context 可以直接再后续新增一个内容 就是描述本次变化的部分 只有哪些部分变了才会加进去 不变就不新增 

  然后后续一旦召回了新的我们之前上下文中不存在的topic 那就在后续追加即可。

  那么还有一个问题 llm的上下文总是有上限的 那么这个候选的主题不可能一直增加下去 这里有一个解决方案 那就是我们每次都记录下最终发送给 llm 做决策的哪些 communit topic 给他们来一个引用计数 可以写到redis里，然后对于哪些 被llm 判断 直接认为可以复用的主题 也通过一个字段进行引用计数。相当于有两个字段  一个记录被rag召回排序之后 选出来的 community topic ，还有一个是被llm决策 认为可以重复利用的communit topic 主题，他们都要统计次数

  当我们的候选上下文多到一定程度 ，例如打到 30个的时候就自动做一轮压缩，可以利用我们先前统计出来的数据 上一轮的数据中 选举10个出来作为种子 直接作为候选的上下文 + 本次rag召回排序后的候选 他们做去重后 作为本次llm的候选，之所以通过这种算法 是因为 如果他们近期被频繁引用 或者 频繁被召回 就说明是近期的热点数据，那么下一次召回他们的几率就比较大 所以提前把他们作为缓存 作为llm的候选，下一次再基于他们做扩展 可以减少新增community topic的概率。之后我们会情况上一轮的计数，重新开始计数，因为我们的数据只统计上一轮的，这属于热点数据

  相当于我们的候选由两部分做成：
  1. 任务运行过程中出现的高频主题，他们在主题数达到上限时 进行重新选举，选举出来的内容都是本轮被高频使用的内容，用于初始缓存
  2. 后续rag召回精排过后 可能会出现种子缓存后续不存在的内容，这时候就需要将他按照我们先前的规则追加到后续名单中，用于llm的上下文
  有了这套机制，我们的llm后续可以很大概率的命中缓存 每次只有新增的候选 或者 被修改过的后续才会需要重新计算，可以大幅度的减少miss cache。当上后续数量过多时，我们又会重建候选名单，防止一些陈旧 无用的 主题长期浪费上下文。这里我们也可以进一步做优化，那就是如果发现重建后的候选名单 跟 旧的名单重叠率大于70%，那可以跳过重建，选择继续复用，因为这说明旧的cache中 还是有大量的 候选主题被频繁引用 我们的缓存机制就不用重建 节省更多token

